== README

To start running the software, simply download the project and run rake db:migrate and then rails server.

* Ruby version - 2.1

* System dependencies - Besides the ruby gems, PhantomJS to run javascript tests with Capybara

* How to run the test suite: tests are in Rspec and Cucumber. To run then simply run rspec (or rake spec) and cucumber (or rake cucumber)
