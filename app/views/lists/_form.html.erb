<%= simple_form_for(@list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :is_private %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
